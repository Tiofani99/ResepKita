<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developer_ngapak.resepkita">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_baru"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ResepTheme">
        <activity android:name=".ui.about.DeveloperDetail"></activity>
        <activity android:name=".ui.about.AboutUsActivity"/>
        <activity android:name=".ui.add_recipe.AddRecipeActivity" />
        <activity android:name=".ui.detail.DetailActivity" />
        <activity android:name=".ui.search.ShowDataActivity" />
        <activity
            android:name=".ui.register.RegisterActivity"
            android:theme="@style/ResepTheme1" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/ResepTheme1" />
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/ResepDashboard" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/ResepTheme1" />
        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:theme="@style/ResepTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>